\section{Datasets}
\label{sec:data}
We evaluate our method on the following two datasets:
%\subsubsection{Movie Review Dataset (MR)}
%The Movie Review dataset~\cite{maas2011learning} is a common benchmark classification dataset that contains movie reviews, that should be classified as either positive or negative. On an average the reviews are about 200 words long. It has balanced train and test sets containing 25k examples each.
\subsubsection{Stanford Sentiment Treebank (SST2)}
The Stanford Sentiment Treebank~\cite{socher2013recursive} is a common benchmark classification dataset that contains movie reviews, to be classified according to their sentiment. On an average the reviews are about 200 words long. The dataset contains train, dev, test splits and binary sentiment labels. 
\subsubsection{Books Intent Classification Dataset}
We use a proprietary dataset of around 60k annotated utterances of users interacting with a popular digital assistant about books related functionality. Each utterance is manually labeled with the user intent e.g., search for a book, read a book and others, with the task being to predict the user intent from a set of 20 intents. We choose 50k utterances as our training data, 5k as test data and 5k as dev data.

%We perform all our experiments on 1 Tesla K80 gpu, use Adam as the optimizer with learning rate of 0.001. We use dropout of 0.4 between layers and L2 regularization with weight of 0.005. 
%We plot the validation fine tuned accuracy and compression ratio's in Figure~\ref{plot:intent} and Figure~\ref{plot:mr}. Results on the test set are shown in Table~\ref{results}. We show the baseline accuracy on top for each experiment (compression ratio 0). We show the initial test accuracy after SVD and final test accuracy after fine tuning.