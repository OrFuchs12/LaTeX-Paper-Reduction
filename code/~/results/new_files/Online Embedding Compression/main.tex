\def\year{2019}\relax
%File: formatting-instruction.tex
\documentclass[letterpaper]{article} %DO NOT CHANGE THIS
\usepackage{aaai19}  %Required
\usepackage{times}  %Required
\usepackage{helvet}  %Required
\usepackage{courier}  %Required
\usepackage{url}  %Required
\usepackage{graphicx}  %Required
\usepackage{times}
\usepackage{caption}
\usepackage{multirow}
\usepackage{ifthen}
\usepackage{enumitem}
\usepackage{todonotes}
\usepackage{changepage}
\usepackage{amsmath}
\usepackage{caption}
\usepackage{multirow}
\usepackage{ifthen}
\usepackage{enumitem}
\usepackage{float}
\usepackage{placeins}
\usepackage{amsfonts}
\usepackage{mathtools}
\usepackage{booktabs}
\usepackage{subcaption}
\DeclarePairedDelimiter\ceil{\lceil}{\rceil}
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}
\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}
\usepackage{wrapfig}
\algnewcommand\algorithmicforeach{\textbf{for each}}
\algdef{S}[FOR]{ForEach}[1]{\algorithmicforeach\ #1\ \algorithmicdo}
\frenchspacing  %Required
\setlength{\pdfpagewidth}{8.5in}  %Required
\setlength{\pdfpageheight}{11in}  %Required
%PDF Info Is Required:
  \pdfinfo{
/Title ()
/Author (AAAI Press Staff)}
\setcounter{secnumdepth}{2}  
 \begin{document}
% The file aaai.sty is the style file for AAAI Press 
% proceedings, working notes, and technical reports.
%
%\title{On Low Memory Footprint Deep NLP Models \\
%	Preserving Accuracy through Spectral Compression Aware Training}   
%\title{Compression of Text Classification Models using Low Rank Matrix Factorization }
%\title{Lossless Embedding  using Low Rank Matrix Factorization }
%\title{Using Low Rank Matrix Factorization for NLP Model Compression }    
\title{Online Embedding Compression for Text Classification using\\
	  Low Rank Matrix Factorization}
  \author{Anish Acharya \thanks{Corresponding Author} \textsuperscript{1,3}, Rahul Goel \textsuperscript{1}, Angeliki Metallinou \textsuperscript{1}, Inderjit Dhillon \textsuperscript{2,3} \\ \textsuperscript{1} Amazon Alexa AI , \textsuperscript{2} Amazon Search Technologies, \textsuperscript{3} University of Texas at Austin\\  {achanish, goerahul, ametalli \}@amazon.com}, isd@a9.com }
\maketitle
\begin{abstract}
%% As deep learning models are becoming ubiquitous and finding applications
%% across domains, labeled training data has become a bottleneck in more wide use
%% of such models. Transfer learning is a simple and elegant way to approach this
%% problem. In nlp applications word embeddings have been the most successful
%% application of transfer learning. However these word embeddings are memory
%% extensive limiting their deployment in devices with low memory resources. In
%% this work we explore 2 ways to reduce the memory footprint of the network:
%% spectral compression (low rank matrix factorization) and quantization. We  
%% compare the memory savings, adaptation time to a new domain as well as
%% accuracy degradation between the 2 and observe that spectral compression gives
%% us more memory savings, better adaptation time as well as minimal accuracy
%% degradation after fine tuning.
%% Current %%%
%%Deep learning models have become state of the art for natural language processing (NLP) tasks. However deploying these models into production systems including embedded devices poses significant memory constraints. This work focuses on model size reduction for deep learning models. We propose a compression method based on low rank matrix factorization, focusing on compressing the word embedding matrix which represents the size bottleneck for many NLP models. Our models are trained,compressed and then further re-trained on the task data to recover accuracy while maintaining the reduced size. Our method achieves 90\% model size reduction with minimal impact in accuracy for a variety of sentence classification tasks on both benchmark and proprietary datasets. We also compare with popular alternative compression methods, like fixed-point-quantization or offline word embedding compression and show better performance at similar compression rate. 
%% Anish %%%
Deep learning models have become state of the art for natural language processing (NLP) tasks, however deploying these models in production system poses significant memory constraints. Existing compression methods are either lossy or introduce significant latency. We propose a compression method that leverages low rank matrix factorization during training,to compress the word embedding layer which represents the size bottleneck for most NLP models. Our models are trained, compressed and then further re-trained on the downstream task to recover accuracy while maintaining the reduced size. Empirically, we show that the proposed method can achieve 90\% compression with minimal impact in accuracy for sentence classification tasks, and outperforms alternative methods like fixed-point quantization or offline word embedding compression. We also analyze the inference time and storage space for our method through FLOP calculations, showing that we can compress DNN models by a configurable ratio and regain accuracy loss without introducing additional latency compared to fixed point quantization. Finally, we introduce a novel learning rate schedule, the Cyclically Annealed Learning Rate (CALR), which we empirically demonstrate to outperform other popular adaptive learning rate algorithms on a sentence classification benchmark.
% We also introduce a novel learning rate scheduling alrorithm we call CALR (cyclically annealed learning rate). We compare CALR to other popularadaptive learning rate scheduling algorithms and show that it leads to better performance on SST2 benchmark dataset.
%We compare ACLR with other popular adaptive learning rates and show that it leads to better performance. 
%% Another major contribution of this paper is a novel simulated annealing inspired adaptive learning rate scheduling algorihtm we call ACLR (annealed cyclic learning rate) which we intuitively argue to be more likely to escape saddle points and converge at a better local minima compared to other popular adaptive learning rates.
%% Deep learning models have become state of the art for natural language
%% applications. However deploying these models in production systems including
%% embedded devices with low memory resources poses significant memory
%% constraints. This work focuses on accuracy preserving model size reduction for
%% deep NLP models. We propose a spectral compression method based on low rank
%% matrix factorization of the model weights and the word embedding matrix. Our
%% models are trained, compressed and then further re-trained on the task data in
%% an attempt to recover accuracy while maintaining the reduced size.  On sentence
%% classification task in both binary and multi class setting, our method achieves
%% over 80\% model size reduction with minimal effect in final model accuracy while
%% significantly accelarating training, and inference.  In some cases our approach
%% shows further improvement in accuracy after re-training while weight
%% quantization causes accuracy degradations that often cannot be recovered.

\end{abstract}
\input{intro}
\input{related}
%\input{background}
\input{method}
\input{analysis}
%\input{models}
%\input{data}
\input{result}
\vspace{-1mm}
\input{conclusion}
\vspace{-3mm}
\bibliography{compression}
\bibliographystyle{aaai}
\end{document}
